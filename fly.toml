# Fly.io config for the Quad Tank Streamlit app.
# Public URL after deploy: https://griffith-pse-quad-tank.fly.dev
# Custom domain (set up later): https://quadtank.griffith-pse.com

app = "griffith-pse-quad-tank"
primary_region = "ord"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "1gb"
